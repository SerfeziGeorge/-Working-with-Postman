npm i express body-parser dotenv express mongoose morgan nodemon slugify validator bcryptjs jsonwebtoken nodemailer express-rate-limit express-mongo-sanitize helmet hpp xss-clean
npm i eslint eslint-config-airbnb eslint-config-prettier eslint-plugin-import eslint-plugin-jsx-a11y eslint-plugin-node eslint-plugin-prettier eslint-plugin-react prettier ndb --save-dev

### In this project we manually tested the aplication using postman. In the folder named, postmanDataPrintScreen, we can find the print screens.

### Unfortunatly it seems that we have an error with the merged route in the routes folder, file categoryRoutes "router.use('/:categoryId/products', productRouter);". The server does not see the route.
